name: Angular CI Pipeline

on:
  push:
    branches:
      - 'feat/*'
      - 'fix/*'
      - 'feature/*'

jobs:
  call-ci-workflow:
    uses: ./.github/workflows/ci-angular.yml
    with:
      branch-name: ${{ github.ref_name }}
      build-path: dist/vyracare-app-shell

  open-pr-develop:
    runs-on: ubuntu-latest
    needs: call-ci-workflow
    if: startsWith(github.ref, 'refs/heads/feat/') || startsWith(github.ref, 'refs/heads/fix/') || startsWith(github.ref, 'refs/heads/feature/')
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Open Pull Request to develop
        uses: repo-sync/pull-request@v2
        with:
          github_token: ${{ secrets.PAT_TOKEN }}   # Personal Access Token com permissão de repo
          source_branch: ${{ github.ref_name }}    # branch de origem (quem fez o push)
          destination_branch: develop              # branch de destino
          pr_title: "Merge ${{ github.ref_name }} into develop"
          pr_body: |
            PR automática criada pelo pipeline.
            Branch de origem: `${{ github.ref_name }}`
