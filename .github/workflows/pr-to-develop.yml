name: PR TO DEVELOP

on:
  push:
    branches:
      - 'feat/*'
      - 'fix/*'
      - 'feature/*'

jobs:
  ci-angular:
    uses: ./.github/workflows/ci-angular.yml
    with:
      branch-name: ${{ github.ref_name }}
